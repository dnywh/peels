"use client";

type ProfileEditFieldProps = {
  label?: string;
  value?: string;
  onChange?: (value: string) => void;
};

function ProfileEditField(_props: ProfileEditFieldProps) {
  return <div />;
}

export default ProfileEditField;
