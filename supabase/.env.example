# Deprecated: use supabase/functions/.env instead (official Supabase location).
# That file is loaded automatically by `supabase start`.
#
#   cp supabase/functions/.env.example supabase/functions/.env
#
# Keep this stub so older docs/commands that mention supabase/.env still point
# maintainers in the right direction.
